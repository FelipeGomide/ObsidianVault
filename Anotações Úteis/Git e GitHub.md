## Início:

- Instalar ferramenta CLI do GitHub e Git
- `gh auth` para autenticar na conta do github pelo browser
- procedimento normal a partir do git:
	- definir login e e-mail
	- git init
	- touch .gitignore
	- git remote add "nome para remote(origin)" "url"
	- git push -u origin branchname